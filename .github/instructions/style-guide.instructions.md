---
description: Naming conventions for TypeScript interfaces.
applyTo: '**/*.{ts,tsx}'
---

# Naming Conventions

## Interfaces

- Do not prefix interface names with `I` (use `CacheManager`, not `ICacheManager`).
- Use PascalCase for interface names.
